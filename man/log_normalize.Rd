% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weights.R
\name{log_normalize}
\alias{log_normalize}
\title{Normalise scores into selection probabilities (logit-based)}
\usage{
log_normalize(x)
}
\arguments{
\item{x}{numeric vector of scores in [0, 1]}
}
\value{
a numeric vector of the same length summing to 1
}
\description{
Maps scores in \verb{(0, 1)} (higher is better) to selection probabilities via a
shifted logit, normalised to sum to 1. The maximize counterpart of
\code{\link[=inverse_normalize]{inverse_normalize()}}; falls back to uniform weights when all scores are
equal.
}
\examples{
log_normalize(c(0.6, 0.8, 0.95))
}
