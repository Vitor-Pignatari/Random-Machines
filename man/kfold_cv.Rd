% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resample.R
\name{kfold_cv}
\alias{kfold_cv}
\title{K-fold cross-validation split in the train/test convention}
\usage{
kfold_cv(n, K = 5, y = NULL, p = 0.75)
}
\arguments{
\item{n}{number of observations}

\item{K}{number of folds; \code{K = 1} selects holdout mode (see section above)}

\item{y}{optional response vector (length \code{n}); stratifies when provided}

\item{p}{training proportion used \strong{only} in holdout mode (\code{K = 1});
ignored otherwise. Default \code{0.75}.}
}
\value{
\code{list(train, test)} of n-by-K logical matrices (\code{TRUE} = selected);
n-by-1 in holdout mode
}
\description{
Produces a fold assignment and returns it as the logical \code{train}/\code{test}
matrices the rest of the pipeline consumes (as \code{\link[=simple_bs]{simple_bs()}} does): column
\code{k} selects, for fold \code{k}, the training rows (\code{train}) and the held-out rows
(\code{test}). When a response vector \code{y} is supplied the folds are stratified so
each class is spread evenly across folds; otherwise they are assigned at
random (suitable for regression).
}
\section{Holdout mode (\code{K = 1})}{

\code{K = 1} is a special case: instead of \code{K} folds it produces a single
train/test split, putting a proportion \code{p} of the rows in \code{train} and the
rest in \code{test}. When \code{y} is supplied the split is stratified per class (each
class contributes \code{p} of its rows to training); otherwise rows are sampled at
random. The return shape is identical to the K-fold case — \code{train}/\code{test} are
just one-column matrices — so callers (e.g. \code{\link[=KernelSamples]{KernelSamples()}}, \code{svm_fit_any})
need no special handling. This subsumes the old \code{simpleHoldout()}.
}

\examples{
# 5-fold stratified CV over a 3-class response
cv <- kfold_cv(n = 150, K = 5, y = iris$Species)
# single stratified 80/20 holdout
ho <- kfold_cv(n = 150, K = 1, y = iris$Species, p = 0.8)
}
