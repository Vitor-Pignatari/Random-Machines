% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/svm-methods.R
\name{svmFit}
\alias{svmFit}
\alias{svmFit,KernelSamples-method}
\alias{svmFit,BootSamples-method}
\title{Fit SVMs across a resampling scheme}
\usage{
svmFit(samples, specs, svmcalls, metric_function, ...)

\S4method{svmFit}{KernelSamples}(samples, specs, svmcalls, metric_function, ...)

\S4method{svmFit}{BootSamples}(samples, specs, svmcalls, metric_function, indexes, ...)
}
\arguments{
\item{samples}{a \code{KernelSamples} or \code{BootSamples} object (drives dispatch)}

\item{specs}{an ArgSpecs object; drives per-case \code{\link[=svmPredict]{svmPredict()}} dispatch}

\item{svmcalls}{list of ksvm calls from \code{.call_builder()}}

\item{metric_function}{metric applied to (truth, hard prediction)}

\item{...}{extra arguments; the \code{BootSamples} method requires \code{indexes}, the
length-B vector of lambda-sampled kernel indices (one per replicate)}
}
\value{
per-kernel list of \code{list(fit, predict, metrics)} (\code{KernelSamples}), or
a single such list (\code{BootSamples})
}
\description{
Internal generic dispatched on the resample object. A \link{KernelSamples} runs
every kernel across every CV fold (stage 1, lambdas); a \link{BootSamples} fits one
lambda-sampled kernel per bootstrap replicate (stage 2, omegas). Prediction is
routed through \code{\link[=svmPredict]{svmPredict()}}, so probabilistic models yield probability
matrices and vote models yield class factors.
}
\section{Methods (by class)}{
\itemize{
\item \code{svmFit(KernelSamples)}: Stage 1 (lambdas): every kernel across every CV fold.

\item \code{svmFit(BootSamples)}: Stage 2 (omegas): one lambda-sampled kernel per bootstrap
replicate. \code{indexes} (length B) selects the kernel for each replicate.

}}
