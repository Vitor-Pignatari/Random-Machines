% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict-methods.R
\name{predict,BootOmegas-method}
\alias{predict,BootOmegas-method}
\title{Predict from a bootstrap ensemble on unseen data}
\usage{
\S4method{predict}{BootOmegas}(object, newdata, specs, ...)
}
\arguments{
\item{object}{a \code{BootOmegas} ensemble}

\item{newdata}{a data.frame of observations to predict; must contain the
predictor columns used at fit time}

\item{specs}{the \code{ArgSpecs} the ensemble was fit from; drives task/prob
dispatch. \code{BootOmegas} no longer stores it (Decision K1), so it must be
supplied -- \code{predict(RandomMachines)} passes its own \verb{@specs}.}

\item{...}{unused}
}
\value{
a numeric vector (regression), a class factor (majority vote), or a
class-probability matrix (probability average)
}
\description{
Scores \code{newdata} with every fitted bootstrap model and aggregates the
per-model predictions into a single ensemble prediction, weighted by the
models' omegas (normalised to sum to 1). The aggregation is dispatched by
task via \code{\link[=rmAggregate]{rmAggregate()}}: weighted mean (regression), weighted majority vote
(classification, \code{prob = FALSE}), or weighted probability average
(classification, \code{prob = TRUE}).
}
