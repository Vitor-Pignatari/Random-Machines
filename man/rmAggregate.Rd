% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/svm-methods.R
\name{rmAggregate}
\alias{rmAggregate}
\alias{rmAggregate,ArgSpecsReg-method}
\alias{rmAggregate,ArgSpecsClassif-method}
\title{Aggregate per-model predictions into an ensemble prediction}
\usage{
rmAggregate(specs, predictions, weights, ...)

\S4method{rmAggregate}{ArgSpecsReg}(specs, predictions, weights, ...)

\S4method{rmAggregate}{ArgSpecsClassif}(specs, predictions, weights, ...)
}
\arguments{
\item{specs}{an ArgSpecs object; drives dispatch via its subclass and \code{prob}}

\item{predictions}{a list (length B) of per-model \code{\link[=svmPredict]{svmPredict()}} outputs}

\item{weights}{a numeric vector (length B) of normalised model weights}

\item{...}{unused; present for method extensibility}
}
\value{
the aggregated prediction: a numeric vector (regression), a class
factor (majority vote), or a class-probability matrix (probability average)
}
\description{
Combines the predictions of the individual bootstrap models into a single
ensemble prediction, weighted by the models' omegas. Dispatches on the
\link[=ArgSpecs-class]{ArgSpecs} subclass so each case aggregates appropriately:
weighted mean (regression), weighted majority vote (classification,
\code{prob = FALSE}), or weighted probability average (classification,
\code{prob = TRUE}).
}
\section{Methods (by class)}{
\itemize{
\item \code{rmAggregate(ArgSpecsReg)}: Regression: weighted mean of the numeric predictions.

\item \code{rmAggregate(ArgSpecsClassif)}: Classification (binary and multiclass): weighted
majority vote (\code{prob = FALSE}) or weighted probability average
(\code{prob = TRUE}). Shared by both via the virtual \code{ArgSpecsClassif} parent.

}}
