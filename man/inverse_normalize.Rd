% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weight-utils.R
\name{inverse_normalize}
\alias{inverse_normalize}
\title{Normalise error metrics into selection probabilities}
\usage{
inverse_normalize(x)
}
\arguments{
\item{x}{numeric vector of error metrics (>= 0)}
}
\value{
a numeric vector of the same length summing to 1
}
\description{
The regression counterpart of \code{\link[=log_normalize]{log_normalize()}}. Where \code{log_normalize}
expects a score in \verb{(0, 1)} (higher is better), this maps an error/loss
metric (lower is better) to selection probabilities via the inverse error,
then normalises to sum to 1. It is scale-invariant (multiplying every error
by a constant leaves the probabilities unchanged) and falls back to uniform
weights when every error is equal or zero.
}
\examples{
placeholder
}
